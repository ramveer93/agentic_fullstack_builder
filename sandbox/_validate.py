import app

# Instantiate the app to validate construction without launching
try:
    app.
    print("Gradio app constructed successfully.")
except Exception as e:
    print(f"Error constructing Gradio app: {str(e)}")